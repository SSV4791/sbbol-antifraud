apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vs-egress-fraud-https
spec:
  exportTo:
    - .
  gateways:
    - mesh
    - gw-egressgateway
  hosts:
    - ${FRAUD_ADAPTER_HOST}
  http:
    - match:
        - gateways:
            - mesh
          port: 80
      rewrite:
        authority: ${FRAUD_ADAPTER_HOST}
      route:
        - destination:
            host: svc-egressgateway
            port:
              number: ${EGRESS_HTTPS_PORT}
          weight: 100
    - match:
        - gateways:
            - gw-egressgateway
          port: ${EGRESS_HTTPS_PORT}
      route:
        - destination:
            host: ${FRAUD_ADAPTER_HOST}
            port:
              number: ${FRAUD_ADAPTER_PORT}
          weight: 100