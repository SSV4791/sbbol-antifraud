apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vs-egress-db-appjournal-4
spec:
  exportTo:
    - .
  gateways:
    - gw-appjournal-tcp-4
    - mesh
  hosts:
    - ${APPJOURNAL_HOST_4}
  tcp:
    - match:
        - gateways:
            - mesh
          port: ${APPJOURNAL_PORT_PROXY_4}
      route:
        - destination:
            host: svc-egressgateway
            port:
              number: ${APPJOURNAL_PORT_LISTENER_4}
          weight: 100
    - match:
        - gateways:
            - gw-appjournal-tcp-4
          port: ${APPJOURNAL_PORT_LISTENER_4}
      route:
        - destination:
            host: ${APPJOURNAL_HOST_4}
            port:
              number: ${APPJOURNAL_PORT}
          weight: 100