apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vs-egress-fluentbit-https
spec:
  exportTo:
    - .
  gateways:
    - mesh
    - gw-egressgateway
  hosts:
    - ${FLUENT_BIT_HTTPS_HOST}
  http:
    - match:
        - gateways:
            - mesh
          port: 80
      rewrite:
        authority: ${FLUENT_BIT_HTTPS_HOST}
      route:
        - destination:
            host: svc-egressgateway
            port:
              number: ${EGRESS_OTT_PORT}
          weight: 100
    - match:
        - gateways:
            - gw-egressgateway
          port: ${EGRESS_OTT_PORT}
      route:
        - destination:
            host: ${FLUENT_BIT_HTTPS_HOST}
            port:
              number: 443
          weight: 100