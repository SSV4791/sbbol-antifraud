apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vs-egress-db-appjournal-5
spec:
  exportTo:
    - .
  gateways:
    - gw-appjournal-tcp-5
    - mesh
  hosts:
    - ${APPJOURNAL_HOST_5}
  tcp:
    - match:
        - gateways:
            - mesh
          port: ${APPJOURNAL_PORT_PROXY_5}
      route:
        - destination:
            host: svc-egressgateway
            port:
              number: ${APPJOURNAL_PORT_LISTENER_5}
          weight: 100
    - match:
        - gateways:
            - gw-appjournal-tcp-5
          port: ${APPJOURNAL_PORT_LISTENER_5}
      route:
        - destination:
            host: ${APPJOURNAL_HOST_5}
            port:
              number: ${APPJOURNAL_PORT}
          weight: 100