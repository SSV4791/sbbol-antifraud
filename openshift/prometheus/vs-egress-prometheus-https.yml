apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vs-egress-prometheus-https
spec:
  exportTo:
    - .
  gateways:
    - mesh
    - gw-egressgateway
  hosts:
    - ${APPMON_HOST}
  http:
    - match:
        - gateways:
            - mesh
          port: 80
      rewrite:
        authority: ${APPMON_HOST}
      route:
        - destination:
            host: svc-egressgateway
            port:
              number: ${EGRESS_HTTPS_PORT}
          weight: 100
    - match:
        - gateways:
            - gw-egressgateway
          port: ${EGRESS_HTTPS_PORT}
      route:
        - destination:
            host: ${APPMON_HOST}
            port:
              number: ${APPMON_HOST_PORT}
          weight: 100